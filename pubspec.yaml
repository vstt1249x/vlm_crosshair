name: vlm_crosshair
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.16.0
  shared_preferences: ^2.3.5
  image_picker: ^1.1.2
  image_cropper: ^8.0.0
  crypto: ^3.0.3  # 👈 THÊM DÒNG NÀY ĐỂ MÃ HÓA

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_gen_runner: ^5.3.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/donate.jpg
    - assets/images/LogoV.png